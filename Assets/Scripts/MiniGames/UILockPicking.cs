using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UILockPicking : MonoBehaviour
{
    [SerializeField] private float[] _sliderSpeed = new float[4] { 2, 4, 6, 8}; //possible speed of sliders

    // Update is called once per frame
    void Update()
    {
        
    }
}
